<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude cache directory from backup -->
    <exclude domain="cache" />
    <!-- Exclude temporary database files -->
    <exclude domain="database" path="music_database-wal" />
    <exclude domain="database" path="music_database-shm" />
</full-backup-content>